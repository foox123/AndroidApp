# Minimal Android App
Minimal Android app built with GitHub Actions CI/CD.

## Build APK
Every push to main/master triggers a build. APK is available as artifact and release.

## Download APK
Go to **Actions** tab → latest run → download `app-debug` artifact.
